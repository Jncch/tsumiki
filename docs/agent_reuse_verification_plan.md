# タスク実行エージェント自動生成における「知識層再利用」検証 方針・計画

## 0. このドキュメントの位置づけ

本書は、これまでの検討で確定した方針と、最小検証の設計、参考にできる既存OSSと評価指標を整理したものである。
結論を先に置く構成とし、未確定事項は末尾にまとめる。表現は検討時のものを保持し、勝手な言い換えは行わない。

---

## 1. 結論サマリ

- 作るのは「汎用の自律エージェント自動生成フレームワーク（ゼロベース）」ではない。これは既存研究の再発明であり非効率なため不採用。
- 中核の価値は「自動生成」そのものではなく「生成された部品の再利用（転用）」に置く。
- 再利用が成立する粒度は明確に分ける。再利用できるのは知識層とツール層であり、最適化済みポリシー層はタスクごとに作り直し＋再評価が必要。
- この仮説の一般原理は既に実証済み（スキルライブラリ系研究）。未検証なのは「業務文書・法務のような縦ドメインで、転用関係にある2タスク間に効くか」という具体形。
- したがって本検証の正味の価値は「既知の原理を未検証のドメインで確認し、負の転移の有無と工数削減効果を実測する」ことにある。
- 最終成果物の輪郭（2026-06-19 確定）: **目的駆動の自動構成 OSS フレームワーク**。自然言語の目的を受け、構造化スキーマへ変換し、評価器を LLM で生成しユーザーが承認、ナレッジを流用または抽出し、エージェントを構成して実行・評価まで一気通貫で行う。
- ゴール設計の決定事項: 目的入力は自然言語 + LLM 構造化スキーマの確認フロー、評価器は LLM 自動生成 + 承認 + 流用蓄積、評価器の中身は決定関数および LLM judge（後者はペアワイズ・3 体パネル・人手較正のガードレール必須）、流用蓄積の検索粒度は `domain` + `task_class` + 入出力スキーマ一致。
- 制約（CLAUDE.md と整合）: 自動構成は AgentSquare のモジュール探索（制約された役割合成）まで、評価器の自動探索は「ユーザー承認済み評価器が存在する状態」でのみ起動、LLM judge を含む評価器にはガードレールを自動適用。

---

## 2. 方針（採用・不採用の決定事項）

| 項目 | 決定 | 理由 |
| --- | --- | --- |
| 汎用フレームワークの自作 | 不採用 | AFlow / ADAS / AgentSquare / A2Flow 等で飽和。限界貢献が小さい |
| 既存OSSの fork + 差分貢献 | 採用 | 再現性と比較可能性を保てる |
| 自律エージェント群のオーケストレーション自動生成 | 当面の対象外 | 探索空間が広く不安定・評価困難。後段の研究課題に切り出す |
| 制約されたマルチエージェント（役割ノードの合成まで） | 採用 | 既存探索手法の射程内で、必要に応じてマルチ化される |
| 差別化の置き場所 | アルゴリズムではなく「整備されたドメイン縦資産」 | 知識・ツール層が転用の実体だから |

---

## 3. 中核仮説と三層分離

「ドメイン知識領域は再利用可能」を1語で扱わず、再利用粒度ごとに3層へ分解する。

| 層 | 内容 | 再利用 | 再評価の要否 |
| --- | --- | --- | --- |
| 静的知識資産 | 法令、条項タクソノミー、ポリシー文書、過去事例。RAGコーパス／ナレッジベース | 可（タスク非依存） | 不要（鮮度管理は別途必要） |
| ツール／インターフェース | 条項検索、管轄ルール照合、引用チェック、フォーマッタ等の決定的関数群 | 可（入出力契約が同じ範囲） | 不要（単体テストで担保） |
| ドメイン特化ポリシー | 役割定義、観点リスト、最適化済みプロンプト・ノード構成 | 半再利用（種は流用、最適化はやり直し） | 必要（タスク目的が変わると劣化） |

設計上の含意:

- バージョン管理の境界を分ける。知識・ツール層はドメイン資産として長期管理。ポリシー層はタスクごとの使い捨て成果物として管理。両者を混ぜない。
- 評価ループに乗せるのはポリシー層の振る舞いのみ。知識・ツール層は単体テスト（関数の正しさ、検索の再現率）で担保しタスク評価から外す。
- 正味の工数削減は「知識収集・ツール実装の再投資」分のみ。ポリシー設計・チューニング・評価は毎回かかる。知識整備が重いドメインほど旨味が大きい。

---

## 4. 既知と未検証の切り分け

### 4.1 既に実証されている（一般原理）

- スキルライブラリ系の研究系列（Voyager を起点）が、知識・スキル層がタスクを越え、さらにエージェントやモデルを越えて転移し得ることを実証している。
- 「能力（知識・スキル）をモデル重みから切り離す」という設計思想が共通前提になっている。
- 同時に、最適化済みプロンプト（ポリシー）はモデルやタスク分布のずれで劣化するという知見もあり、ポリシー層の作り直しが必要という主張を裏付ける。

### 4.2 未検証（本検証の対象）

- 既存ベンチマークはゲーム、web操作、embodied、QA、金融推論などに偏る。
- 業務文書・法務の縦ドメインで、転用関係にある2タスク間（例: チェックと作成）に三層分離が効くかは確認できていない。
- 留意: 関連研究の多くは2026年の新しいプレプリントで査読前を含む。「強いが若い」証拠として扱う。

---

## 5. 検証設計

### 5.1 前提原則（スレッド全体の合意事項）

- 評価器が先、自動生成は後。探索や再合成は最大化するスカラースコアが無ければ起動しない。
- 評価条件は実験前に固定する（後出ししない）。
- 再利用は評価を免除しない。再合成しても新タスクの評価信号で検証し直す。

### 5.2 フェーズ構成

| フェーズ | 内容 | ゲート |
| --- | --- | --- |
| Phase 0 データ・ラベル監査 | 対象データの粒度（文書単位か条項単位か）、件数、クラス比、ラベル付与基準の一貫性を確認 | 粒度次第でポリシー最適化ループの可否が決まる |
| Phase 1 評価器＋ベースライン | 主指標を計算する評価関数を実装。単一プロンプトのベースラインを作る。test分割を層化固定、temperature=0、モデルバージョン固定、全試行をMLflowに記録 | ベースラインのスコア・コスト・レイテンシが以降の比較の床 |
| Phase 2 再利用の対照実験 | T2をゼロベースで構築 vs T1の知識・ツール層を注入して構築 | 知識層再利用の効果を実測 |
| Phase 3 頑健性・負の転移 | タスク記述の言い換え・シード変更で再実行し安定性を確認。再利用層が新タスクの足を引っ張らないか確認 | 不安定なら再現性の観点で否定的結論 |
| Phase 4 ハイブリッド戦略 | クラウド強モデルで生成した辞書をローカル弱モデルで使う構成を検証 | 辞書品質と実行モデル能力のどちらがボトルネックかを判定 |
| Phase 5a 辞書 ablation | NDA で description / excerpt_examples / 対象条項などの要素を削った variant 辞書を作り、reuse paired diff への寄与を計測 | Knowledge schema 設計の根拠が固まる |
| Phase 5b 可搬資産化 | 既存 YAML 辞書を Agent Skills 標準（Markdown スキル）に変換するコンバータを実装、配布可能な形に整える | reuse paired diff が同等を保てば資産化成立 |
| Phase 5c 目的駆動 CLI | 自然言語目的→構造化スキーマ→評価器生成→ナレッジ流用→エージェント構成→実行・評価 の end-to-end パイプライン雛形を NDA で動かす。承認済み評価器を eval/generated/ に蓄積 | MLflow に Phase 2 と同じ指標が出ること、生成評価器が NDA で Phase 2 値と±0.05 内で一致 |
| Phase 6 横展開 | 別ドメイン1本（暫定: ISO27001 監査チェック）で T1 / T2 を定義、reuse vs zerobase 対照実験を再走 | reuse paired diff > 0 を再現できれば N=2 で汎化主張可 |
| Phase 7 フレーム化 | AgentSquare を fork し、Knowledge / Tool 層を差し替え可能にするプラグイン仕様を実装。examples/nda と examples/iso27001 が同一フレームで動くことを確認 | 同一フレームでの 2 ドメイン動作が成立 |
| Phase 8 発信 | Zenn Part 3「辞書を可搬資産にする」、Part 4「別ドメインで再現した」執筆。OSS 公開（README / CONTRIBUTING / ライセンス整備） | リポジトリ公開と Zenn 記事公開 |

### 5.3 対照実験の核

- 設定: 1ドメイン、転用関係にある2タスク（T1, T2）。
- 比較: 「T2 ゼロベース構築」対「T2 = T1の知識・ツール層を注入して構築」。
- 測るもの: 目標到達までの工数・反復回数（コールドスタート差）、最終スコア、負の転移の有無と大きさ。

### 5.4 合格・判断ルール（実験前に固定）

- 知識層再利用がゼロベースに対しコールドスタート工数を有意に削減し、最終スコアを劣化させず、負の転移が出ない → 仮説確認。縦1本で前進。
- ゼロベースと差が無い、または負の転移が出る → そのドメイン・タスク対では再利用前提が成立しない、という明確な結論。

---

## 6. 参考OSS

| 名称 | 役割・位置づけ | 本検証での使い方 | 出典 |
| --- | --- | --- | --- |
| AFlow | コード表現ワークフローをMCTSで探索する自動最適化。Operator（Generate / Format / Review & Revise / Ensemble / Test / Programmer） | ポリシー層の自動探索エンジン候補。fork して差分で貢献 | github.com/FoundationAgents/AFlow / arXiv 2410.10762 |
| AgentSquare | モジュール設計空間（Planning / Reasoning / Tool Use / Memory）の探索。module evolution と recombination、性能予測器、グローバル経験プール | 知識・ツール層を固定しポリシー層のみ再合成する分離設計の土台 | github.com/tsinghua-fib-lab/AgentSquare / arXiv 2410.06153 |
| ADAS (Meta Agent Search) | コード空間で新エージェントを生成・archive・反復改善 | R&Dの横口（AFlowが頭打ちのときのみ）。本番探索には不採用 | github.com/ShengranHu/ADAS / arXiv 2408.08435 |
| A2Flow | 自己適応的な抽象Operatorとオペレータメモリで手作りOperator依存を低減 | Operator硬さ対策の参照（自作は追実装になる点に注意） | arXiv 2511.20693 |
| MaAS | スーパーネットからクエリ依存の構成をサンプリング。クロスデータセット転移 | 「タスクに応じた構成生成」の参照（arXiv ID未確認） | 論文名で参照 |
| Voyager | スキルライブラリ・パラダイムの起点。コードスキルの蓄積と環境間転移 | 知識・スキル層再利用の原典 | voyager.minedojo.org |
| Agent Skills（Markdownスキル標準） | ルーティングメタデータ・段階的開示・コードテンプレートを持つ可搬なMarkdownスキルファイル | 知識・スキル層の形式に流用。Claude上でそのまま使える | ASDA論文参照 / arXiv 2603.16112 |
| DSPy（+ MIPROv2） | 評価駆動のプロンプト・重み最適化。MLflowに自動記録のflavorあり | ポリシー層の再最適化に使用。自前実装は不要 | github.com/stanfordnlp/dspy |
| Braintrust / LangSmith / Langfuse | 評価・実験・観測のプラットフォーム | 評価ループの記録・比較に利用 | 各公式 |

### 参考（スキル再利用・転移の手法。指標設計の流用元。多くは2026年の新しいプレプリント）

| 名称 | 内容 | 出典 |
| --- | --- | --- |
| EXPEL | 成功・失敗の軌跡から再利用可能な洞察を抽出し永続プールに蓄積。ゲーム外へ一般化 | 既存研究 |
| Skill-Pro | in-domain / cross-task / cross-agent でのスキル再利用率と性能を評価 | arXiv 2602.01869 |
| SkillGraph | グラフ構造でスキル再利用を高め冗長性を減らし、単純から複雑タスクへ転移 | arXiv 2605.12039 |
| MUSE-Autoskill | スキル単位のメモリ、単体テスト駆動の改善、クロスエージェント転移の明示的検証 | arXiv 2605.27366 |
| EvoSkill | スキルを訓練タスクとモデルから切り離した自己完結フォルダ化。プロンプト転移は劣化しやすいと指摘 | arXiv 2603.02766 |
| ASDA | 金融推論ドメインで、誤り分析から推論パターンを蒸留。Agent Skills標準と互換 | arXiv 2603.16112 |
| Adaptation of Agentic AI（サーベイ） | post-training・メモリ・スキルの全体整理 | arXiv 2512.16301 |

---

## 7. 評価指標まとめ

### 7.1 ラベルが定義できるタスク（分類・チェック系）

| 指標 | 用途 | 備考 |
| --- | --- | --- |
| NG Recall（見逃し率の裏返し） | 主指標 | 業務上の致命傷はNGをOK判定する見逃しのため |
| Precision | 補助 | 全件NG判定への偏りを抑える |
| F1 / F-beta（beta>1） | 補助・合成 | Recall重視で1スカラーに合成する場合 |
| 混同行列 | 補助 | 誤りの内訳確認 |
| Accuracy | 参考のみ | クラス不均衡下では単独使用しない |
| Latency | コスト面 | 構成比較に必須 |
| Cost（USD） | コスト面 | 探索・実行のトークン費用 |

多目的化する場合は、コスト・レイテンシ・頑健性をペナルティ項として1スカラーに合成する（ADASが未解決と挙げた multi-objective 方向）。意味の薄い項目の追加はノイズになるため避ける。

### 7.2 再利用の対照実験

| 指標 | 用途 | 由来 |
| --- | --- | --- |
| 再利用率（reuse rate） | 知識・ツール層がどれだけ再使用されたか | Skill-Pro 等 |
| クロスタスク成功差（cross-task success delta） | ゼロベース対再利用の最終性能差 | スキル再利用系 |
| コールドスタート工数（目標到達までの反復回数） | 立ち上げ工数の削減効果 | 本検証の主目的 |
| 負の転移（negative transfer）の有無・大きさ | 再利用層が新タスクの足を引っ張らないか | 本検証のリスク管理 |
| 冗長性（redundancy） | ライブラリ肥大の確認 | SkillGraph |

### 7.3 開放タスク（アイデア発想など。今回の主土俵ではないが参考）

| 指標・手法 | 用途 | 注意 |
| --- | --- | --- |
| ペアワイズ / ELO（Idea Arena型） | 絶対採点より人間選好に整合 | 単一スカラーへの落とし込みに一手間 |
| 複数批評パネル（3体以上） | 単一判定器の偏り低減 | ハッキングは完全には防げない |
| 埋め込みベースの多様性・新規性 | 1案収束を防ぐガードレール | 単独では不十分 |
| 人手較正（判定器と人間の一致度） | 判定器の妥当性確認 | 一致度が低ければループ無効 |

開放タスクでは、生のLLM判定スカラーへ直接最適化しない。報酬ハッキング（見栄えだけ良く中身の空虚な出力への収束）を招くため。

---

## 8. リスクと留意点

- 知識層の再利用は「正しさの再利用」ではない。法令・ポリシーは改定されるため、知識資産に鮮度管理（出典・更新日・有効性）が必須。古い知識の再利用はポリシーが正しくても誤出力を生む。
- ツール層の流用は入出力契約が同じ範囲でのみ安全。タスク変更で必要な入出力が変われば改修対象。
- 自動設計手法全般に頑健性の弱さ（タスク記述の言い換えで結果がぶれる）が報告されている。再現性最優先の観点で、合否は単発の好スコアで決めない。
- 探索中のコストは大きい。検証時は validation セットを小さく絞る。
- AFlowはOperatorに移行起因のバグが残る可能性があり、デバッグ工数を見込む。

---

## 9. 当初確定すべきだった事項（Phase 1〜4 で完了）

補注: 以下は本書執筆当初に未確定だった項目。Phase 1〜4 完了時点で次の通り確定済み。詳細は `docs/experiments/` 配下の各報告を参照する。

1. 対象ドメイン: NDA（中小企業庁ガイドラインの guideline02.docx 系を雛形に合成）。
2. T1 / T2: T1 = NG 条項の検出、T2 = NG 条項の修正。
3. データ粒度: 条項単位、9 NG パターン、合成クリーン条項からの注入で生成。
4. 主指標: T1 は NG Recall を主、F-beta 補助。T2 は target NG が修正後に検出されない率（success_rate）と negative_transfer（target 外への波及）。

---

## 10. OSS 化ロードマップ（Phase 5〜8）

Phase 1〜4 で「弱モデル + 知識層再利用は強く効く（paired diff +0.212）」「強モデルでは zerobase 自身が高性能で相対価値は縮小」「クラウド辞書をローカルで使っても弱モデルの天井は破れない」が確定した。これを踏まえ、tsumiki を **層分離をプラグイン化した OSS フレーム** として外部公開するまでの段階を以下に置く。

### 10.1 最終成果物の輪郭

ディレクトリ構成（2026-06-19 更新、目的駆動への再設計を反映）:

```
tsumiki/                         # AgentSquare fork ベース
├── goal/                        # 自然言語目的 → 構造化スキーマ変換、評価器ジェネレータ、承認フロー
├── input/                       # PDF / DOCX / MD / YAML / JSONL ローダ
├── knowledge/
│   ├── extractors/              # LLM ベースの構造化抽出（RAG なし）
│   ├── schemas/                 # NG パターン等の共通 schema（汎用）
│   └── skills/                  # Agent Skills 標準で配布する可搬資産
├── tools/                       # 検索・比較・diff・フォーマッタ等の決定的関数群
├── policy/
│   ├── compose/                 # 制約付き役割合成（AgentSquare モジュール探索）
│   └── optimize/                # DSPy / AFlow による Policy 再最適化
├── eval/
│   ├── core/                    # LLM judge 用ガードレール（ペアワイズ / 3 体パネル / 人手較正）
│   ├── generated/               # 流用蓄積。承認済み評価器をドメイン × タスククラスで分類
│   │   └── <domain>/<task_class>/<evaluator_id>/  # evaluator.py / meta.yaml / test_cases.jsonl / README.md
│   └── runners/                 # MLflow 連動の自動検証ランナー
├── runner/                      # 目的入力→構造化→評価器生成→ナレッジ→構成→実行・評価 の end-to-end CLI
└── examples/
    ├── nda/                     # リファレンス実装1（Phase 5c で整備）
    └── iso27001/                # リファレンス実装2（Phase 6 で整備）
```

汎用フレームは `tsumiki/` 直下、ドメイン固有資産は `examples/` 配下。`examples/` を 2 件以上揃えることで「ドメイン非依存に使える」の裏付けとする。
`eval/generated/` への初期蓄積は Phase 1〜4 で実装した NDA 評価器（`modification.success_rate`, `modification.negative_transfer`, `classification.ng_recall` 等）の移植で行う。

### 10.2 「目的入力」「自動構成」「自動検証」の射程

§2 の決定および §9 の禁則と整合させる。2026-06-19 のゴール再整理を反映。

| 項目 | 採用範囲 | 採用しない範囲 |
| --- | --- | --- |
| 目的入力 | 自然言語入力 → LLM による構造化スキーマ変換 → ユーザー確認 / 修正フロー | フォーム固定入力のみ、自然言語のまま暗黙解釈 |
| 自動構成 | AgentSquare のモジュール探索（制約された役割合成）まで | 完全自律のエージェント生成・オーケストレーション |
| 評価器 | LLM が下書きを生成 → ユーザーが承認 → 確定した評価器でのみ Policy 探索を起動。承認済みは `eval/generated/` に蓄積し流用 | 評価器が未承認のまま自動探索を回す（§9 と整合） |
| 評価器の中身 | 決定関数（Python 計算）+ LLM judge（ペアワイズ / 3 体パネル / 人手較正のガードレール必須） | 生の LLM 判定スカラーに直接最適化 |
| 流用蓄積の検索粒度 | `domain` + `task_class` + 入出力スキーマ一致のみ流用候補に提示 | 自然言語の embedding 類似度マッチによる柔軟流用（Phase 7 以降の最適化） |
| ナレッジ化 | LLM ベースの構造化抽出 → YAML / Agent Skills | RAG コーパスの構築 |

### 10.3 「シナリオ非依存」を主張するための検証基準

| 基準 | 内容 |
| --- | --- |
| N ≥ 2 ドメインで仮説再現 | NDA と ISO27001 の 2 件で reuse paired diff > 0 を再現 |
| 同一フレームで動く | examples/nda と examples/iso27001 が tsumiki/ パッケージの同一コードで動く |
| 目的駆動セットアップ | 自然言語の目的入力から、構造化スキーマ → 評価器生成 → ナレッジ流用 → エージェント構成 まで一気通貫で動く |
| `schemas/` の抽象化 | NDA の ng_patterns と ISO27001 の compliance_gaps が同じスキーマ系列に乗る |
| 評価器流用の検証 | NDA で承認済み評価器が ISO27001 でも流用候補として提示され、一致すれば再評価して採用、不一致は新規生成 |
| 再現コマンドの整備 | 第三者が `git clone → uv sync → 1 コマンドで 2 件を再現実行` できる |

### 10.4 各フェーズ詳細

| Phase | 主指標 / 完了条件 | 主成果物 | 期間目安 |
| --- | --- | --- | --- |
| Phase 5a 辞書 ablation | 各構成要素の reuse paired diff への寄与順位が決まる | `docs/experiments/phase5a_ablation_*.md`、ablation 用 variant 辞書 | 1〜2週 |
| Phase 5b 可搬資産化 | 変換前後で reuse paired diff が同等（誤差内）であること | `src/tsumiki/knowledge/skills/`、コンバータ実装 | 1週 |
| Phase 5c 目的駆動 CLI | 自然言語目的入力から end-to-end 実行が完了、生成評価器が Phase 2 値と±0.05 内で一致、承認済み評価器が eval/generated/ に保存される | `src/tsumiki/goal/`、`src/tsumiki/eval/core/`、`src/tsumiki/eval/generated/nda/`、`src/tsumiki/runner/` | 3週 |
| Phase 6 横展開 | ISO27001 で reuse paired diff > 0、真の negative_transfer ≒ 0 | `data/raw/iso27001/`、`docs/experiments/phase6_*.md`、`examples/iso27001/` | 3〜4週 |
| Phase 7 フレーム化 | examples/nda と examples/iso27001 が tsumiki/ の同一コードで動作 | AgentSquare fork、`tsumiki/` パッケージ初期版 | 2〜3週 |
| Phase 8 発信 | Zenn Part 3 / Part 4 公開、GitHub リポジトリ公開 | Zenn 連載継続、`README.md` / `CONTRIBUTING.md` / `LICENSE` | 2週 |

### 10.5 リスクと対応

| リスク | 対応 |
| --- | --- |
| AgentSquare fork の上流追随コスト | 差分を最小に保つ。プラグイン点を限定し、本体改変を避ける |
| ISO27001 ドメインのデータ品質（オープン情報からの合成） | Phase 0 同等のラベル監査を Phase 6 冒頭で踏む |
| 「自動構成」「自動検証」の射程に対する誤解 | README に明示。完全自律生成を謳わない |
| Phase 6 で reuse paired diff が立たない場合 | 「NDA で効いたが ISO27001 では効かない」を OSS の章として正直に報告し、適用条件を Knowledge schema に書き戻す（§5.4 「ゼロベースと差が無い → 明確な結論」の運用） |
| Knowledge 鮮度（§7 ガードレール） | `schemas/` に出典・更新日・有効性フィールドを必須化する |

---

## 11. 出典・参考URL

| 項目 | URL |
| --- | --- |
| AFlow（リポジトリ） | https://github.com/FoundationAgents/AFlow |
| AFlow（論文） | https://arxiv.org/abs/2410.10762 |
| AgentSquare（リポジトリ） | https://github.com/tsinghua-fib-lab/AgentSquare |
| AgentSquare（論文） | https://arxiv.org/abs/2410.06153 |
| ADAS（リポジトリ） | https://github.com/ShengranHu/ADAS |
| ADAS（論文） | https://arxiv.org/abs/2408.08435 |
| A2Flow（論文） | https://arxiv.org/abs/2511.20693 |
| Voyager（プロジェクト） | https://voyager.minedojo.org |
| DSPy（リポジトリ） | https://github.com/stanfordnlp/dspy |
| Skill-Pro | https://arxiv.org/abs/2602.01869 |
| SkillGraph | https://arxiv.org/abs/2605.12039 |
| MUSE-Autoskill | https://arxiv.org/abs/2605.27366 |
| EvoSkill | https://arxiv.org/abs/2603.02766 |
| ASDA | https://arxiv.org/abs/2603.16112 |
| Adaptation of Agentic AI（サーベイ） | https://arxiv.org/abs/2512.16301 |

注: 2602 / 2603 / 2605 系のarXiv IDは2026年の新しいプレプリントであり、査読前を含む。MaASはarXiv IDを本書では未確認とする。
